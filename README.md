# streamlit-car-price-dash
자동차 구매 금액 예측하는 대시보드 앱

 실행 방법
 ```bash
 pip install -r requirements.txt
 streamlit run app.py
 ```

 변경 사항 (디자인 개선)
 - 공통 스타일과 페이지 설정을 위한 `ui_helpers.py` 추가
 - `app.py`, `app_home.py`, `app_eda.py`, `app_ml.py` UI/레이아웃 개선
 - `requirements.txt`에 `streamlit`, `joblib` 추가
